#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *_isupper(): Decides if c is uppercase or not.
 * Return: Always 0.
 */
int _isupper(c)
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}

