#include <stdio.h>
#include "main.h"
/**
 * main - funcion main
 * @argc: number of commands
 * @argv: strings
 * Return: number of arguments
*/
int main(int argc, char *argv[])
{
    printf("%s\n", argv[0]);
    return (0);
}
